# Customization

Primary extension points for buyers:

- `config/platform.php` feature toggles
- Add new modules via `php artisan make:module <Name>`
- Override pipelines in `config/auth_pipeline.php`
- Extend permissions in `config/rbac.php`

