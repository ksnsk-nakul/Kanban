# RBAC

RBAC is implemented in phases. The permission registry lives in `config/rbac.php`.

Next phase adds:

- Permission sync command
- Middleware `permission:<key>`
- Policy helpers
- Blade directives

